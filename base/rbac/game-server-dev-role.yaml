apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: game-server-dev-role
  namespace: game-server-dev
rules:
- apiGroups: [""]
  resources: ["pods", "services"]
  verbs: ["get", "list", "watch"]
